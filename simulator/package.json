{
  "name": "simulator",
  "version": "1.0.0",
  "description": "For testing purposes: Sends random data every 2 seconds (replacing a real IoT device and providing data like temperature, humidity, etc) to the MQTT broker",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"No tests specified\" && exit 0",
    "lint": "echo \"No linting configured\" && exit 0",
    "dev": "node index.js"
  },
  "author": "Mauro Pimentel",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.7",
    "mqtt": "^5.0.0"
  }
}
